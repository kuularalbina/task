#include <stdio.h>

int main(){
    char symbol = 'A';
    int number = 42;
    double decimal = 2.71828;

    printf("Символ: %c, Целое число: %d, Десятичное число: %.5f", symbol, number, decimal);

    return 0;
}