#include <stdio.h>

int main(){
    char symbol = 'A';
    int number = 42;
    double decimal = 2.71828;

    printf("char (symbol): %c\n", symbol);
    printf("int (number): %d\n", number);
    printf("double (decimal): %.5f\n", decimal);

    return 0;
}